<pu:WindowX x:Class="UIBrowser.Views.MainView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:pu="clr-namespace:Panuon.UI.Silver;assembly=Panuon.UI.Silver"
            xmlns:local="clr-namespace:UIBrowser.Views"
            mc:Ignorable="d"
            Title="MainView"
            Height="700"
            Width="1200">
    <pu:WindowXCaption.Header>
        <TextBlock VerticalAlignment="Center"
                   FontSize="20"
                   Margin="8,0,0,0"
                   FontFamily="Microsoft YaHei UI Light">
            <Run>P A N</Run>
            <Run Foreground="#68D450"> U O N</Run>
        </TextBlock>
    </pu:WindowXCaption.Header>

    <Grid>
        <Button Click="Button_Click_1"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Height="30"
                Width="30" />
        <Border Width="300"
                Height="300"
                BorderBrush="LightGray"
                BorderThickness="1">
            <pu:Drawer x:Name="Dr"
                       MaxWidth="200"
                       MinWidth="20"
                       Placement="Left"
                       HorizontalAlignment="Left">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="20"/>
                    </Grid.ColumnDefinitions>
                    <Grid Background="#EEEEEE">
                        <TextBlock Text="Drawer"
                                   Margin="15,15,0,0" />
                    </Grid>
                    <ToggleButton Grid.Column="1"
                                  Content="&gt;"
                                  pu:ToggleButtonHelper.ToggleButtonStyle="Link"
                                  pu:ToggleButtonHelper.CheckedContent="&lt;"
                                  IsChecked="{Binding IsOpen, ElementName=Dr}"/>
                </Grid>
            </pu:Drawer>
        </Border>


    </Grid>
</pu:WindowX>
