<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Panuon.UI.Silver"
                    xmlns:converter="clr-namespace:Panuon.UI.Silver.Converters">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Panuon.UI.Silver;component/Resources/Resource.xaml" />
        <ResourceDictionary Source="pack://application:,,,/Panuon.UI.Silver;component/Resources/Converter.xaml" />
        <ResourceDictionary Source="pack://application:,,,/Panuon.UI.Silver;component/Themes/ScrollViewer.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <converter:TreeViewVerticalChainConverter x:Key="TreeViewVerticalChainConverter" />

    <Style TargetType="{x:Type TreeView}">
        <Setter Property="SnapsToDevicePixels"
                Value="{StaticResource Default_SnapsToDevicePixels}" />
        <Setter Property="FontSize"
                Value="{StaticResource Default_FontSize}" />
        <Setter Property="SnapsToDevicePixels"
                Value="{StaticResource Default_SnapsToDevicePixels}" />
        <Setter Property="local:TreeViewHelper.ItemHeight"
                Value="40" />
        <Setter Property="local:TreeViewHelper.SelectedBackground"
                Value="#888888" />
        <Setter Property="BorderBrush"
                Value="Transparent" />
        <Setter Property="BorderThickness"
                Value="0" />
        <Setter Property="VerticalContentAlignment"
                Value="Top" />
        <Setter Property="HorizontalContentAlignment"
                Value="Stretch" />
        <Setter Property="local:TreeViewHelper.TreeViewStyle"
                Value="Standard" />
        <Setter Property="VirtualizingStackPanel.IsVirtualizing"
                Value="True" />
        <Setter Property="VirtualizingStackPanel.VirtualizationMode"
                Value="Recycling" />
        <Setter Property="local:ScrollViewerHelper.ScrollBarCornerRadius"
                Value="3" />
        <Setter Property="local:ScrollViewerHelper.ScrollBarThickness"
                Value="6" />
        <Setter Property="local:IconHelper.FontFamily"
                Value="/Panuon.UI.Silver;component/Resources/#fontawesome" />
        <Setter Property="local:IconHelper.FontSize"
                Value="{Binding FontSize, Converter={StaticResource Add3Converter},RelativeSource={RelativeSource Self},Mode=OneWay}" />
        <Setter Property="local:IconHelper.Foreground"
                Value="{Binding Foreground,RelativeSource={RelativeSource Self},Mode=OneWay}" />
        <Setter Property="local:IconHelper.Width"
                Value="NaN" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TreeView">
                    <Border Name="Border"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Background="{TemplateBinding Background}">
                        <ScrollViewer Focusable="False"
                                      VerticalScrollBarVisibility="{Binding Path=(ScrollViewer.VerticalScrollBarVisibility),RelativeSource={RelativeSource AncestorType=TreeView}, Mode=OneWay}"
                                      HorizontalScrollBarVisibility="{Binding Path=(ScrollViewer.HorizontalScrollBarVisibility),RelativeSource={RelativeSource AncestorType=TreeView}, Mode=OneWay}"
                                      Padding="{TemplateBinding Padding}"
                                      local:ScrollViewerHelper.ScrollBarCornerRadius="{Binding Path=(local:ScrollViewerHelper.ScrollBarCornerRadius),RelativeSource={RelativeSource AncestorType=TreeView}, Mode=OneWay}"
                                      local:ScrollViewerHelper.ScrollBarThickness="{Binding Path=(local:ScrollViewerHelper.ScrollBarThickness),RelativeSource={RelativeSource AncestorType=TreeView}, Mode=OneWay}">
                            <ItemsPresenter />
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Path=(local:TreeViewHelper.TreeViewStyle),RelativeSource={RelativeSource Self}, Mode=OneWay}"
                         Value="Classic">
                <Setter Property="local:TreeViewHelper.ItemPadding"
                        Value="10,0,0,0" />
                <Setter Property="local:TreeViewHelper.SelectedBackground"
                        Value="#DDDDDD" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Path=(local:TreeViewHelper.TreeViewStyle),RelativeSource={RelativeSource Self}, Mode=OneWay}"
                         Value="Modern">
                <Setter Property="BorderBrush"
                        Value="#3E3E3E" />
                <Setter Property="local:TreeViewHelper.ItemPadding"
                        Value="10,0,0,0" />
                <Setter Property="local:TreeViewHelper.SelectedBackground"
                        Value="#DDDDDD" />
            </DataTrigger>
            <Trigger Property="IsEnabled"
                     Value="False">
                <Setter Property="Opacity"
                        Value="0.4" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type TreeViewItem}">
        <Setter Property="SnapsToDevicePixels"
                Value="{StaticResource Default_SnapsToDevicePixels}" />
        <Setter Property="FontSize"
                Value="{StaticResource Default_FontSize}" />
        <Setter Property="Foreground"
                Value="{Binding Foreground,RelativeSource={RelativeSource AncestorType=TreeView}}" />
        <Setter Property="SnapsToDevicePixels"
                Value="{StaticResource Default_SnapsToDevicePixels}" />
        <Setter Property="VerticalContentAlignment"
                Value="Center" />
        <Setter Property="Background"
                Value="Transparent" />
        <Setter Property="Cursor"
                Value="Hand" />
        <Setter Property="Foreground"
                Value="{Binding Foreground,RelativeSource={RelativeSource AncestorType=TreeView}}" />
        <Setter Property="Padding"
                Value="{Binding Path=(local:TreeViewHelper.ItemPadding),RelativeSource={RelativeSource AncestorType=TreeView}}" />
        <Setter Property="local:IconHelper.Width"
                Value="{Binding Path=(local:IconHelper.Width),RelativeSource={RelativeSource AncestorType=TreeView},Mode=OneWay}" />
        <Setter Property="local:IconHelper.Foreground"
                Value="{Binding Path=(local:IconHelper.Foreground),RelativeSource={RelativeSource AncestorType=TreeView},Mode=OneWay}" />
        <Setter Property="local:IconHelper.FontSize"
                Value="{Binding Path=(local:IconHelper.FontSize),RelativeSource={RelativeSource AncestorType=TreeView},Mode=OneWay}" />
        <Setter Property="local:IconHelper.FontFamily"
                Value="{Binding Path=(local:IconHelper.FontFamily),RelativeSource={RelativeSource AncestorType=TreeView},Mode=OneWay}" />
        <Setter Property="local:TreeViewHelper.ItemHeight"
                Value="{Binding Path=(local:TreeViewHelper.ItemHeight),RelativeSource={RelativeSource AncestorType=TreeView},Mode=OneWay}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding Path=(local:TreeViewHelper.TreeViewStyle),RelativeSource={RelativeSource AncestorType=TreeView}, Mode=OneWay}"
                         Value="Standard">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TreeViewItem}">
                            <Border BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Background="{TemplateBinding Background}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="{Binding Path=(local:TreeViewHelper.ItemHeight),RelativeSource={RelativeSource AncestorType=TreeViewItem},Mode=OneWay,Converter={StaticResource GridLengthConverter}}" />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="{Binding Path=(local:IconHelper.Width),RelativeSource={RelativeSource AncestorType=TreeViewItem},Mode=OneWay,Converter={StaticResource GridLengthConverter}}" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <ToggleButton x:Name="Toggle"
                                                  Width="12"
                                                  Margin="0,0,3,0"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  HorizontalAlignment="Left"
                                                  Foreground="{TemplateBinding Foreground}"
                                                  ClickMode="Press"
                                                  IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent">
                                                    <ContentPresenter VerticalAlignment="Center"
                                                                      HorizontalAlignment="Center" />
                                                </Border>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                        <ToggleButton.Content>
                                            <Grid x:Name="arrow"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"
                                                  Width="{TemplateBinding Height}"
                                                  Height="{TemplateBinding Height}">
                                                <Path x:Name="arrow1"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"
                                                      Data="M 4,3 L10,7.5 L 4,12 Z"
                                                      Width="15"
                                                      Height="15"
                                                      Stroke="{x:Null}"
                                                      Fill="{TemplateBinding Foreground}"
                                                      RenderTransformOrigin="0.5,0.5">
                                                </Path>
                                                <Path x:Name="arrow2"
                                                      Opacity="0"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"
                                                      Data="M 4,3 L10,7.5 L 4,12 Z"
                                                      Width="15"
                                                      Height="15"
                                                      Stroke="{x:Null}"
                                                      Fill="{TemplateBinding Foreground }"
                                                      RenderTransformOrigin="0.5,0.5">
                                                    <Path.RenderTransform>
                                                        <RotateTransform Angle="45"></RotateTransform>
                                                    </Path.RenderTransform>
                                                </Path>
                                            </Grid>
                                        </ToggleButton.Content>
                                    </ToggleButton>
                                    <Grid x:Name="GrdContent"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          Grid.Column="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Label x:Name="LblIcon"
                                               Content="{Binding Path=(local:TreeViewHelper.ItemIcon),Converter={StaticResource IconConverter},RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                               Foreground="{Binding Path=(local:IconHelper.Foreground),RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                               FontSize="{Binding Path=(local:IconHelper.FontSize),RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                               FontFamily="{Binding Path=(local:IconHelper.FontFamily),RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                               VerticalContentAlignment="Center"
                                               HorizontalContentAlignment="Center"
                                               Padding="10,0,7,0" />
                                        <ContentPresenter x:Name="PART_Header"
                                                          Grid.Column="1"
                                                          ContentSource="Header"
                                                          Margin="{TemplateBinding Padding}"
                                                          VerticalAlignment="Center" />
                                    </Grid>
                                    <ItemsPresenter x:Name="ItemsHost"
                                                    Grid.Row="1"
                                                    Grid.Column="1"
                                                    Visibility="Collapsed" />
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <DataTrigger Binding="{Binding Path=(local:TreeViewHelper.ExpandMode), RelativeSource={RelativeSource AncestorType=TreeView}}"
                                             Value="SingleClick">
                                    <Setter TargetName="Toggle"
                                            Property="IsEnabled"
                                            Value="False" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Path=(local:TreeViewHelper.ItemIcon), RelativeSource={RelativeSource Self}}"
                                             Value="{x:Null}">
                                    <Setter TargetName="LblIcon"
                                            Property="Visibility"
                                            Value="Collapsed" />
                                </DataTrigger>
                                <Trigger Property="IsExpanded"
                                         Value="False">
                                    <Setter TargetName="ItemsHost"
                                            Property="Visibility"
                                            Value="Collapsed" />
                                </Trigger>
                                <Trigger Property="IsExpanded"
                                         Value="True">
                                    <Setter TargetName="ItemsHost"
                                            Property="Visibility"
                                            Value="Visible" />
                                </Trigger>
                                <Trigger Property="IsExpanded"
                                         Value="True">
                                    <Setter TargetName="arrow1"
                                            Property="Opacity"
                                            Value="0" />
                                    <Setter TargetName="arrow2"
                                            Property="Opacity"
                                            Value="1" />
                                </Trigger>
                                <Trigger Property="HasItems"
                                         Value="False">
                                    <Setter TargetName="Toggle"
                                            Property="Visibility"
                                            Value="Hidden" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <DataTrigger Binding="{Binding Path=(local:TreeViewHelper.TreeViewStyle),RelativeSource={RelativeSource AncestorType=TreeView}, Mode=OneWay}"
                         Value="Classic">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TreeViewItem}">
                            <Grid>
                                <Border x:Name="BdrMain"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Background="{TemplateBinding Background}">
                                </Border>
                                <Border x:Name="BdrHover"
                                        Opacity="0"
                                        BorderBrush="Transparent"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Background="{Binding Path=(local:TreeViewHelper.SelectedBackground),RelativeSource={RelativeSource AncestorType=TreeView}, Mode=OneWay}">
                                </Border>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="{Binding Path=(local:TreeViewHelper.ItemHeight),RelativeSource={RelativeSource AncestorType=TreeViewItem},Mode=OneWay,Converter={StaticResource GridLengthConverter}}" />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border x:Name="BdrSelected"
                                            Visibility="Collapsed"
                                            BorderBrush="Transparent"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            Background="{Binding Path=(local:TreeViewHelper.SelectedBackground),RelativeSource={RelativeSource AncestorType=TreeView}, Mode=OneWay}">
                                    </Border>
                                    <Grid x:Name="GrdContent"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="{Binding Path=(local:IconHelper.Width),RelativeSource={RelativeSource AncestorType=TreeViewItem},Mode=OneWay,Converter={StaticResource GridLengthConverter}}" />
                                            <ColumnDefinition />
                                        </Grid.ColumnDefinitions>
                                        <Label x:Name="LblIcon"
                                               Content="{Binding Path=(local:TreeViewHelper.ItemIcon),Converter={StaticResource IconConverter},RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                               Foreground="{Binding Path=(local:IconHelper.Foreground),RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                               FontSize="{Binding Path=(local:IconHelper.FontSize),RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                               FontFamily="{Binding Path=(local:IconHelper.FontFamily),RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                               VerticalContentAlignment="Center"
                                               HorizontalContentAlignment="Center"
                                               Padding="10,0,7,0" />
                                        <ContentPresenter x:Name="PART_Header"
                                                          Grid.Column="1"
                                                          Margin="{TemplateBinding Padding}"
                                                          ContentSource="Header"
                                                          VerticalAlignment="Center" />
                                    </Grid>
                                    <ToggleButton x:Name="Toggle"
                                                  Width="12"
                                                  Margin="0,0,10,0"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  HorizontalAlignment="Right"
                                                  Foreground="{TemplateBinding Foreground}"
                                                  ClickMode="Press"
                                                  IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border>
                                                    <ContentPresenter VerticalAlignment="Center"
                                                                      HorizontalAlignment="Center" />
                                                </Border>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                        <ToggleButton.Content>
                                            <Grid x:Name="arrow"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Right"
                                                  Width="{TemplateBinding Height}"
                                                  Height="{TemplateBinding Height}">
                                                <Path x:Name="arrow1"
                                                      Opacity="1"
                                                      VerticalAlignment="Center"
                                                      Grid.Column="1"
                                                      HorizontalAlignment="Center"
                                                      Data="M5,4 H14 L9.5,10 Z"
                                                      Width="15"
                                                      Height="15"
                                                      Fill="{TemplateBinding Foreground}" />
                                                <Path x:Name="arrow2"
                                                      Opacity="0"
                                                      VerticalAlignment="Center"
                                                      Grid.Column="1"
                                                      HorizontalAlignment="Center"
                                                      Data="M5,10 H14 L9.5,4.5 Z"
                                                      Width="15"
                                                      Height="15"
                                                      Fill="{TemplateBinding Foreground}" />
                                            </Grid>
                                        </ToggleButton.Content>
                                    </ToggleButton>
                                    <ItemsPresenter x:Name="ItemsHost"
                                                    Grid.Row="1"
                                                    Visibility="Collapsed" />
                                </Grid>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <DataTrigger Binding="{Binding Path=(local:TreeViewHelper.ExpandMode), RelativeSource={RelativeSource AncestorType=TreeView}}"
                                             Value="SingleClick">
                                    <Setter TargetName="Toggle"
                                            Property="IsEnabled"
                                            Value="False" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Path=(local:TreeViewHelper.ItemIcon), RelativeSource={RelativeSource Self}}"
                                             Value="{x:Null}">
                                    <Setter TargetName="LblIcon"
                                            Property="Visibility"
                                            Value="Collapsed" />
                                </DataTrigger>
                                <Trigger Property="IsExpanded"
                                         Value="False">
                                    <Setter TargetName="ItemsHost"
                                            Property="Visibility"
                                            Value="Collapsed" />
                                </Trigger>
                                <Trigger Property="IsExpanded"
                                         Value="True">
                                    <Setter TargetName="ItemsHost"
                                            Property="Visibility"
                                            Value="Visible" />
                                </Trigger>
                                <Trigger Property="IsExpanded"
                                         Value="True">
                                    <Setter TargetName="arrow1"
                                            Property="Opacity"
                                            Value="0" />
                                    <Setter TargetName="arrow2"
                                            Property="Opacity"
                                            Value="1" />
                                </Trigger>
                                <Trigger Property="HasItems"
                                         Value="False">
                                    <Setter TargetName="Toggle"
                                            Property="Visibility"
                                            Value="Hidden" />
                                </Trigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource Self}}"
                                                   Value="False" />
                                        <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}"
                                                   Value="True" />
                                    </MultiDataTrigger.Conditions>
                                    <MultiDataTrigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="BdrHover"
                                                                 Storyboard.TargetProperty="Opacity"
                                                                 To="0.2"
                                                                 Duration="0:0:0.5" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </MultiDataTrigger.EnterActions>
                                    <MultiDataTrigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="BdrHover"
                                                                 Storyboard.TargetProperty="Opacity"
                                                                 Duration="0:0:0.5" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </MultiDataTrigger.ExitActions>
                                </MultiDataTrigger>
                                <Trigger Property="IsSelected"
                                         Value="True">
                                    <Setter TargetName="BdrSelected"
                                            Property="Visibility"
                                            Value="Visible" />
                                </Trigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource Self}}"
                                                   Value="False" />
                                        <Condition Binding="{Binding IsExpanded, RelativeSource={RelativeSource Self}}"
                                                   Value="True" />
                                        <Condition Binding="{Binding Path=(local:TreeViewHelper.ExpandedBrush),RelativeSource={RelativeSource AncestorType=TreeView},Converter={StaticResource IsNotNullConverter}}"
                                                   Value="True" />
                                    </MultiDataTrigger.Conditions>
                                    <Setter TargetName="BdrMain"
                                            Property="Background"
                                            Value="{Binding Path=(local:TreeViewHelper.ExpandedBrush),RelativeSource={RelativeSource AncestorType=TreeView}, Mode=OneWay}" />
                                </MultiDataTrigger>

                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>

            </DataTrigger>
            <DataTrigger Binding="{Binding Path=(local:TreeViewHelper.TreeViewStyle),RelativeSource={RelativeSource AncestorType=TreeView}, Mode=OneWay}"
                         Value="Modern">
                <Setter Property="Background"
                        Value="#E0E0E0" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TreeViewItem}">
                            <Grid>
                                <Border x:Name="BdrMain"
                                        Background="Transparent"
                                        BorderThickness="0" />
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="{Binding Path=(local:TreeViewHelper.ItemHeight),RelativeSource={RelativeSource AncestorType=TreeViewItem},Mode=OneWay,Converter={StaticResource GridLengthConverter}}" />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="3" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Border x:Name="BdrSelected"
                                            Visibility="Collapsed"
                                            BorderBrush="Transparent"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            Background="{Binding Path=(local:TreeViewHelper.SelectedBackground),RelativeSource={RelativeSource AncestorType=TreeView}, Mode=OneWay}"
                                            Grid.ColumnSpan="2">
                                    </Border>
                                    <Rectangle x:Name="Rect"
                                               Fill="{TemplateBinding Background}" />
                                    <Grid x:Name="GrdContent"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          Grid.Column="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="{Binding Path=(local:IconHelper.Width),RelativeSource={RelativeSource AncestorType=TreeViewItem},Mode=OneWay,Converter={StaticResource GridLengthConverter}}" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Label x:Name="LblIcon"
                                               Content="{Binding Path=(local:TreeViewHelper.ItemIcon),Converter={StaticResource IconConverter},RelativeSource={RelativeSource AncestorType=TreeViewItem}, Mode=OneWay}"
                                               Foreground="{Binding Path=(local:IconHelper.Foreground),RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                               FontSize="{Binding Path=(local:IconHelper.FontSize),RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                               FontFamily="{Binding Path=(local:IconHelper.FontFamily),RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                               VerticalContentAlignment="Center"
                                               HorizontalContentAlignment="Center"
                                               Padding="10,0,7,0"/>
                                        <ContentPresenter x:Name="PART_Header"
                                                          Grid.Column="1"
                                                          ContentSource="Header"
                                                          Margin="{TemplateBinding Padding}"
                                                          VerticalAlignment="Center" />
                                    </Grid>
                                    <ItemsPresenter x:Name="ItemsHost"
                                                    Grid.Row="1"
                                                    Grid.Column="1"
                                                    Margin="-3,0,0,0"
                                                    Visibility="Collapsed" />
                                </Grid>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <DataTrigger Binding="{Binding Path=(local:TreeViewHelper.ItemIcon), RelativeSource={RelativeSource Self}}"
                                             Value="{x:Null}">
                                    <Setter TargetName="LblIcon"
                                            Property="Visibility"
                                            Value="Collapsed" />
                                </DataTrigger>
                                <Trigger Property="IsExpanded"
                                         Value="False">
                                    <Setter TargetName="ItemsHost"
                                            Property="Visibility"
                                            Value="Collapsed" />
                                </Trigger>
                                <Trigger Property="IsExpanded"
                                         Value="True">
                                    <Setter TargetName="ItemsHost"
                                            Property="Visibility"
                                            Value="Visible" />
                                </Trigger>
                                <Trigger Property="IsSelected"
                                         Value="True">
                                    <Setter TargetName="Rect"
                                            Property="Fill"
                                            Value="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=TreeView}}" />
                                    <Setter TargetName="BdrSelected"
                                            Property="Visibility"
                                            Value="Visible" />
                                </Trigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource Self}}"
                                                   Value="False" />
                                        <Condition Binding="{Binding IsExpanded, RelativeSource={RelativeSource Self}}"
                                                   Value="True" />
                                        <Condition Binding="{Binding Path=(local:TreeViewHelper.ExpandedBrush),RelativeSource={RelativeSource AncestorType=TreeView},Converter={StaticResource IsNotNullConverter}}"
                                                   Value="True" />
                                    </MultiDataTrigger.Conditions>
                                    <Setter TargetName="BdrMain"
                                            Property="Background"
                                            Value="{Binding Path=(local:TreeViewHelper.ExpandedBrush),RelativeSource={RelativeSource AncestorType=TreeView}, Mode=OneWay}" />
                                </MultiDataTrigger>

                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>

            </DataTrigger>
            <DataTrigger Binding="{Binding Path=(local:TreeViewHelper.TreeViewStyle),RelativeSource={RelativeSource AncestorType=TreeView}, Mode=OneWay}"
                         Value="Chain">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TreeViewItem}">
                            <Border BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Background="{TemplateBinding Background}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="{Binding Path=(local:TreeViewHelper.ItemHeight),RelativeSource={RelativeSource AncestorType=TreeViewItem},Mode=OneWay,Converter={StaticResource GridLengthConverter}}" />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid Width="13">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Rectangle x:Name="BorderHorizontalChain"
                                                   RenderOptions.EdgeMode="Aliased"
                                                   StrokeThickness="1"
                                                   Stroke="Gray"
                                                   StrokeDashArray="3,2"
                                                   VerticalAlignment="Center"
                                                   Visibility="Visible"
                                                   Margin="-7,0,0,0" />
                                        <ToggleButton x:Name="Toggle"
                                                      Grid.Column="1"
                                                      Width="13"
                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                      HorizontalAlignment="Right"
                                                      Foreground="{TemplateBinding Foreground}"
                                                      ClickMode="Press"
                                                      IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}">
                                            <ToggleButton.Template>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border>
                                                        <ContentPresenter VerticalAlignment="Center"
                                                                          HorizontalAlignment="Center" />
                                                    </Border>
                                                </ControlTemplate>
                                            </ToggleButton.Template>
                                            <ToggleButton.Content>
                                                <Grid x:Name="arrow"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"
                                                      Width="{TemplateBinding Height}"
                                                      Height="{TemplateBinding Height}">
                                                    <TextBlock x:Name="arrow1"
                                                               Text=""
                                                               FontSize="{Binding FontSize,RelativeSource={RelativeSource AncestorType=TreeView}}"
                                                               Foreground="{TemplateBinding Foreground}"
                                                               FontFamily="/Panuon.UI.Silver;component/Resources/#fontawesome" />
                                                    <TextBlock x:Name="arrow2"
                                                               Text=""
                                                               Foreground="{TemplateBinding Foreground}"
                                                               FontSize="{Binding FontSize,RelativeSource={RelativeSource AncestorType=TreeView}}"
                                                               Opacity="0"
                                                               FontFamily="/Panuon.UI.Silver;component/Resources/#fontawesome" />
                                                </Grid>
                                            </ToggleButton.Content>
                                        </ToggleButton>
                                    </Grid>
                                    <Grid x:Name="GrdContent"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          Grid.Column="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="{Binding Path=(local:IconHelper.Width),RelativeSource={RelativeSource AncestorType=TreeViewItem},Mode=OneWay,Converter={StaticResource GridLengthConverter}}" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Label x:Name="LblIcon"
                                               Content="{Binding Path=(local:TreeViewHelper.ItemIcon),Converter={StaticResource IconConverter},RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                               Foreground="{Binding Path=(local:IconHelper.Foreground),RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                               FontSize="{Binding Path=(local:IconHelper.FontSize),RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                               FontFamily="{Binding Path=(local:IconHelper.FontFamily),RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                               VerticalContentAlignment="Center"
                                               HorizontalContentAlignment="Center"
                                               Padding="10,0,7,0" />
                                        <ContentPresenter x:Name="PART_Header"
                                                          Grid.Column="1"
                                                          ContentSource="Header"
                                                          VerticalAlignment="Center"
                                                          Margin="{TemplateBinding Padding}" />
                                    </Grid>
                                    <Rectangle x:Name="BorderVerticalChain"
                                               RenderOptions.EdgeMode="Aliased"
                                               Grid.Row="1"
                                               StrokeThickness="1"
                                               Stroke="Gray"
                                               StrokeDashArray="3,2"
                                               HorizontalAlignment="Center"
                                               Visibility="Collapsed"
                                               Margin="{Binding Path=(local:TreeViewHelper.ItemHeight),RelativeSource={RelativeSource AncestorType=TreeView},Mode=OneWay,Converter={StaticResource TreeViewVerticalChainConverter}}" />
                                    <ItemsPresenter x:Name="ItemsHost"
                                                    Grid.Row="1"
                                                    Grid.Column="1"
                                                    Visibility="Collapsed" />
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <DataTrigger Binding="{Binding Path=(local:TreeViewHelper.ExpandMode), RelativeSource={RelativeSource AncestorType=TreeView}}"
                                             Value="SingleClick">
                                    <Setter TargetName="Toggle"
                                            Property="IsEnabled"
                                            Value="False" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Path=(local:TreeViewHelper.ItemIcon), RelativeSource={RelativeSource Self}}"
                                             Value="{x:Null}">
                                    <Setter TargetName="LblIcon"
                                            Property="Visibility"
                                            Value="Collapsed" />
                                </DataTrigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsExpanded"
                                                   Value="True" />
                                        <Condition Property="HasItems"
                                                   Value="True" />
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="ItemsHost"
                                            Property="Visibility"
                                            Value="Visible" />
                                    <Setter TargetName="BorderVerticalChain"
                                            Property="Visibility"
                                            Value="Visible" />
                                    <Setter TargetName="arrow1"
                                            Property="Opacity"
                                            Value="0" />
                                    <Setter TargetName="arrow2"
                                            Property="Opacity"
                                            Value="1" />
                                </MultiTrigger>
                                <Trigger Property="HasItems"
                                         Value="False">
                                    <Setter TargetName="Toggle"
                                            Property="Visibility"
                                            Value="Collapsed" />
                                </Trigger>
                                <Trigger Property="IsSelected"
                                         Value="True">
                                    <Setter Property="Foreground"
                                            Value="{Binding Path=(local:TreeViewHelper.SelectedBackground),RelativeSource={RelativeSource  AncestorType=TreeView},Mode=OneWay}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding Path=(local:TreeViewHelper.SelectedForeground),RelativeSource={RelativeSource AncestorType=TreeView},Converter={StaticResource IsNotNullConverter}}"
                               Value="True" />
                    <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource Self}}"
                               Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Foreground"
                        Value="{Binding Path=(local:TreeViewHelper.SelectedForeground),RelativeSource={RelativeSource AncestorType=TreeView},Mode=OneWay}" />
            </MultiDataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="TreeViewItem_NonStyle"
           TargetType="{x:Type TreeViewItem}">
        <Setter Property="SnapsToDevicePixels"
                Value="{StaticResource Default_SnapsToDevicePixels}" />
        <Setter Property="FontSize"
                Value="{StaticResource Default_FontSize}" />
        <Setter Property="Padding"
                Value="{Binding Path=(local:TreeViewHelper.ItemPadding),RelativeSource={RelativeSource AncestorType=Menu},Mode=OneWay}" />
        <Setter Property="Height"
                Value="Auto" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TreeViewItem}">
                    <Grid Margin="1">
                        <ContentPresenter x:Name="Presenter"
                                          ContentSource="Header"
                                          Grid.Column="1"
                                          Margin="{TemplateBinding Padding}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>