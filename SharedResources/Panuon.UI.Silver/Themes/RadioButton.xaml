<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Panuon.UI.Silver"
                    xmlns:converter="clr-namespace:Panuon.UI.Silver.Converters">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Panuon.UI.Silver;component/Resources/Resource.xaml" />
        <ResourceDictionary Source="pack://application:,,,/Panuon.UI.Silver;component/Resources/Converter.xaml" />
        <ResourceDictionary Source="pack://application:,,,/Panuon.UI.Silver;component/Themes/ScrollViewer.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <converter:CheckBoxGlyphPathConverter x:Key="CheckBoxGlyphPathConverter" />
    <converter:SwitchToggleMarginConverter x:Key="SwitchToggleMarginConverter" />
    <converter:Minus2Converter x:Key="Minus2Converter" />


    <Style TargetType="{x:Type RadioButton}">
        <Setter Property="SnapsToDevicePixels"
                Value="{StaticResource Default_SnapsToDevicePixels}" />
        <Setter Property="FontSize"
                Value="{StaticResource Default_FontSize}" />
        <Setter Property="local:RadioButtonHelper.GlyphBrush"
                Value="#FFFFFF" />
        <Setter Property="local:RadioButtonHelper.CheckedGlyphBrush"
                Value="#FFFFFF" />
        <Setter Property="local:RadioButtonHelper.BoxHeight"
                Value="16" />
        <Setter Property="local:RadioButtonHelper.BoxWidth"
                Value="16" />
        <Setter Property="local:RadioButtonHelper.CornerRadius"
                Value="2" />
        <Setter Property="Background"
                Value="LightGray" />
        <Setter Property="local:RadioButtonHelper.CheckedBackground"
                Value="#3E3E3E" />
        <Setter Property="BorderThickness"
                Value="0" />
        <Setter Property="VerticalContentAlignment"
                Value="Center" />
        <Setter Property="Foreground"
                Value="#3E3E3E" />
        <Setter Property="Padding"
                Value="5,0,0,0" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding Path=(local:RadioButtonHelper.RadioButtonStyle),RelativeSource={RelativeSource Self}, Mode=OneWay}"
                         Value="Standard">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type RadioButton}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="{Binding Path=(local:RadioButtonHelper.HeaderWidth),RelativeSource={RelativeSource AncestorType=RadioButton}, Converter={StaticResource GridLengthConverter}}" />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <ContentControl x:Name="ContentHeader"
                                                VerticalAlignment="Center"
                                                Content="{Binding Path=(local:RadioButtonHelper.Header),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}" />

                                <Grid Background="Transparent">
                                    <Grid VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid x:Name="Grid"
                                              Width="{Binding Path=(local:RadioButtonHelper.BoxWidth),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                              Height="{Binding Path=(local:RadioButtonHelper.BoxHeight),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}">
                                            <Ellipse x:Name="Ellipse"
                                                     Fill="{TemplateBinding Background}"
                                                     Stroke="{TemplateBinding BorderBrush}"
                                                     StrokeThickness="{TemplateBinding BorderThickness, Converter={StaticResource ThicknessToDoubleConverter}}" />
                                            <Ellipse x:Name="EllipseInner"
                                                     Width="{Binding Path=(local:RadioButtonHelper.BoxWidth),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                                     Height="{Binding Path=(local:RadioButtonHelper.BoxHeight),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                                     Fill="{Binding Path=(local:RadioButtonHelper.GlyphBrush),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                                     StrokeThickness="0"
                                                     RenderTransformOrigin="0.5,0.5">
                                                <Ellipse.RenderTransform>
                                                    <ScaleTransform x:Name="Scale"
                                                                    ScaleX="0.4"
                                                                    ScaleY="0.4" />
                                                </Ellipse.RenderTransform>
                                            </Ellipse>
                                        </Grid>
                                        <ContentPresenter x:Name="Presenter"
                                                          Grid.Column="1"
                                                          ContentSource="Content"
                                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                          Margin="{TemplateBinding Padding}" />
                                    </Grid>
                                </Grid>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsChecked, RelativeSource={RelativeSource Self}}"
                                                   Value="True" />
                                        <Condition Binding="{Binding Path=(local:RadioButtonHelper.CheckedContent),Converter={StaticResource IsNotNullConverter}, RelativeSource={RelativeSource Self}}"
                                                   Value="True" />
                                    </MultiDataTrigger.Conditions>
                                    <Setter TargetName="Presenter"
                                            Property="Content"
                                            Value="{Binding Path=(local:RadioButtonHelper.CheckedContent), RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}" />
                                </MultiDataTrigger>
                                <Trigger Property="IsChecked"
                                         Value="True">
                                    <Setter TargetName="Ellipse"
                                            Property="Fill"
                                            Value="{Binding Path=(local:RadioButtonHelper.CheckedBackground),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}" />
                                    <Setter TargetName="EllipseInner"
                                            Property="Fill"
                                            Value="{Binding Path=(local:RadioButtonHelper.CheckedGlyphBrush),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}" />
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="Scale"
                                                                 Storyboard.TargetProperty="ScaleX"
                                                                 To="0.5"
                                                                 Duration="0:0:0.5">
                                                    <DoubleAnimation.EasingFunction>
                                                        <BackEase EasingMode="EaseOut"></BackEase>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                                <DoubleAnimation Storyboard.TargetName="Scale"
                                                                 Storyboard.TargetProperty="ScaleY"
                                                                 To="0.5"
                                                                 Duration="0:0:0.5">
                                                    <DoubleAnimation.EasingFunction>
                                                        <BackEase EasingMode="EaseOut"></BackEase>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="Scale"
                                                                 Storyboard.TargetProperty="ScaleX"
                                                                 Duration="0:0:0.5">
                                                    <DoubleAnimation.EasingFunction>
                                                        <BackEase EasingMode="EaseOut"></BackEase>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                                <DoubleAnimation Storyboard.TargetName="Scale"
                                                                 Storyboard.TargetProperty="ScaleY"
                                                                 Duration="0:0:0.5">
                                                    <DoubleAnimation.EasingFunction>
                                                        <BackEase EasingMode="EaseOut"></BackEase>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <DataTrigger Binding="{Binding Path=(local:RadioButtonHelper.RadioButtonStyle),RelativeSource={RelativeSource Self}, Mode=OneWay}"
                         Value="Switch">
                <Setter Property="Background"
                        Value="White" />
                <Setter Property="BorderBrush"
                        Value="LightGray" />
                <Setter Property="BorderThickness"
                        Value="1" />
                <Setter Property="local:RadioButtonHelper.GlyphBrush"
                        Value="#FFFFFF" />
                <Setter Property="local:RadioButtonHelper.CheckedBackground"
                        Value="#888888" />
                <Setter Property="local:RadioButtonHelper.CheckedGlyphBrush"
                        Value="#FFFFFF" />
                <Setter Property="local:RadioButtonHelper.BoxHeight"
                        Value="22" />
                <Setter Property="local:RadioButtonHelper.BoxWidth"
                        Value="30" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type RadioButton}">
                            <Grid Background="Transparent">
                                <Grid VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid x:Name="Grid"
                                          Width="{Binding Path=(local:RadioButtonHelper.BoxWidth),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                          Height="{Binding Path=(local:RadioButtonHelper.BoxHeight),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}">
                                        <Rectangle x:Name="RectChecked"
                                                   Height="{Binding Path=(local:RadioButtonHelper.BoxHeight),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                                   Width="{Binding Path=(local:RadioButtonHelper.BoxWidth),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                                   Fill="{Binding Path=(local:RadioButtonHelper.CheckedBackground),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                                   Stroke="{TemplateBinding BorderBrush}"
                                                   StrokeThickness="{TemplateBinding BorderThickness, Converter={StaticResource ThicknessToDoubleConverter}}"
                                                   RadiusX="{Binding Path=(local:RadioButtonHelper.BoxHeight),RelativeSource={RelativeSource AncestorType=RadioButton},Converter={StaticResource HalfConverter}, Mode=OneWay}"
                                                   RadiusY="{Binding Path=(local:RadioButtonHelper.BoxHeight),RelativeSource={RelativeSource AncestorType=RadioButton},Converter={StaticResource HalfConverter}, Mode=OneWay}" />
                                        <Rectangle x:Name="Rect"
                                                   Height="{Binding Path=(local:RadioButtonHelper.BoxHeight),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                                   Width="{Binding Path=(local:RadioButtonHelper.BoxWidth),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                                   Fill="{TemplateBinding Background}"
                                                   Stroke="{TemplateBinding BorderBrush}"
                                                   StrokeThickness="{TemplateBinding BorderThickness, Converter={StaticResource ThicknessToDoubleConverter}}"
                                                   RadiusX="{Binding Path=(local:RadioButtonHelper.BoxHeight),RelativeSource={RelativeSource AncestorType=RadioButton},Converter={StaticResource HalfConverter}, Mode=OneWay}"
                                                   RadiusY="{Binding Path=(local:RadioButtonHelper.BoxHeight),RelativeSource={RelativeSource AncestorType=RadioButton},Converter={StaticResource HalfConverter}, Mode=OneWay}"
                                                   RenderTransformOrigin="0.5,0.5">
                                            <Rectangle.RenderTransform>
                                                <ScaleTransform x:Name="scale"
                                                                ScaleX="1"
                                                                ScaleY="1"></ScaleTransform>
                                            </Rectangle.RenderTransform>
                                        </Rectangle>
                                        <Rectangle x:Name="RectToggle"
                                                   Height="{Binding Path=(local:RadioButtonHelper.BoxHeight),Converter={StaticResource Minus2Converter},RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                                   Width="{Binding Path=(local:RadioButtonHelper.BoxHeight),Converter={StaticResource Minus2Converter},RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                                   StrokeThickness="0"
                                                   HorizontalAlignment="Right"
                                                   VerticalAlignment="Center"
                                                   Fill="{Binding Path=(local:RadioButtonHelper.GlyphBrush),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                                   RadiusX="{Binding Path=(local:RadioButtonHelper.BoxHeight),RelativeSource={RelativeSource AncestorType=RadioButton},Converter={StaticResource HalfConverter}, Mode=OneWay}"
                                                   RadiusY="{Binding Path=(local:RadioButtonHelper.BoxHeight),RelativeSource={RelativeSource AncestorType=RadioButton},Converter={StaticResource HalfConverter}, Mode=OneWay}">
                                            <Rectangle.Margin>
                                                <MultiBinding Converter="{StaticResource SwitchToggleMarginConverter}">
                                                    <Binding Path="Width"
                                                             ElementName="Rect" />
                                                    <Binding Path="Height"
                                                             ElementName="Rect" />
                                                </MultiBinding>
                                            </Rectangle.Margin>
                                            <Rectangle.Effect>
                                                <DropShadowEffect x:Name="effect"
                                                                  Color="DimGray"
                                                                  Opacity="0.4"
                                                                  BlurRadius="3"
                                                                  ShadowDepth="1"
                                                                  Direction="275" />
                                            </Rectangle.Effect>
                                        </Rectangle>
                                    </Grid>
                                    <ContentPresenter x:Name="Presenter"
                                                      Grid.Column="1"
                                                      ContentSource="Content"
                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      Margin="{TemplateBinding Padding}" />
                                </Grid>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsChecked, RelativeSource={RelativeSource Self}}"
                                                   Value="True" />
                                        <Condition Binding="{Binding Path=(local:RadioButtonHelper.CheckedContent),Converter={StaticResource IsNotNullConverter}, RelativeSource={RelativeSource Self}}"
                                                   Value="True" />
                                    </MultiDataTrigger.Conditions>
                                    <Setter TargetName="Presenter"
                                            Property="Content"
                                            Value="{Binding Path=(local:RadioButtonHelper.CheckedContent), RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}" />
                                </MultiDataTrigger>

                                <Trigger Property="IsChecked"
                                         Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ThicknessAnimation Storyboard.TargetName="RectToggle"
                                                                    Storyboard.TargetProperty="Margin"
                                                                    To="0,0,0,0"
                                                                    Duration="0:0:0.3">
                                                    <ThicknessAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut" />
                                                    </ThicknessAnimation.EasingFunction>
                                                </ThicknessAnimation>
                                                <DoubleAnimation Storyboard.TargetName="scale"
                                                                 Storyboard.TargetProperty="ScaleX"
                                                                 To="0"
                                                                 Duration="0:0:0.2">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut" />
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                                <DoubleAnimation Storyboard.TargetName="scale"
                                                                 Storyboard.TargetProperty="ScaleY"
                                                                 To="0"
                                                                 Duration="0:0:0.3">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut" />
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ThicknessAnimation Storyboard.TargetName="RectToggle"
                                                                    Storyboard.TargetProperty="Margin"
                                                                    Duration="0:0:0.3">
                                                    <ThicknessAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut" />
                                                    </ThicknessAnimation.EasingFunction>
                                                </ThicknessAnimation>
                                                <DoubleAnimation Storyboard.TargetName="scale"
                                                                 Storyboard.TargetProperty="ScaleX"
                                                                 Duration="0:0:0.3">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut" />
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                                <DoubleAnimation Storyboard.TargetName="scale"
                                                                 Storyboard.TargetProperty="ScaleY"
                                                                 Duration="0:0:0.3">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut" />
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>

                                            </Storyboard>
                                        </BeginStoryboard>

                                    </Trigger.ExitActions>
                                </Trigger>

                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <DataTrigger Binding="{Binding Path=(local:RadioButtonHelper.RadioButtonStyle),RelativeSource={RelativeSource Self}, Mode=OneWay}"
                         Value="Switch2">
                <Setter Property="Background"
                        Value="White" />
                <Setter Property="BorderBrush"
                        Value="LightGray" />
                <Setter Property="BorderThickness"
                        Value="1" />
                <Setter Property="local:RadioButtonHelper.GlyphBrush"
                        Value="#FFFFFF" />
                <Setter Property="local:RadioButtonHelper.CheckedBackground"
                        Value="#888888" />
                <Setter Property="local:RadioButtonHelper.CheckedGlyphBrush"
                        Value="#FFFFFF" />
                <Setter Property="local:RadioButtonHelper.BoxHeight"
                        Value="22" />
                <Setter Property="local:RadioButtonHelper.BoxWidth"
                        Value="44" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type RadioButton}">
                            <Grid Background="Transparent">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="{Binding Path=(local:RadioButtonHelper.HeaderWidth),RelativeSource={RelativeSource AncestorType=RadioButton}, Converter={StaticResource GridLengthConverter}}" />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <ContentControl x:Name="ContentHeader"
                                                VerticalAlignment="Center"
                                                Content="{Binding Path=(local:RadioButtonHelper.Header),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}" />
                                <Grid Grid.Column="1"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid x:Name="Grid"
                                          Width="{Binding Path=(local:RadioButtonHelper.BoxWidth),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                          Height="{Binding Path=(local:RadioButtonHelper.BoxHeight),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}">
                                        <Border x:Name="RectChecked"
                                                Height="{Binding Path=(local:RadioButtonHelper.BoxHeight),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                                Width="{Binding Path=(local:RadioButtonHelper.BoxWidth),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                                Background="{Binding Path=(local:RadioButtonHelper.CheckedBackground),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="{Binding Path=(local:RadioButtonHelper.CornerRadius),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}" />
                                        <Border x:Name="Rect"
                                                Height="{Binding Path=(local:RadioButtonHelper.BoxHeight),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                                Width="{Binding Path=(local:RadioButtonHelper.BoxWidth),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="{Binding Path=(local:RadioButtonHelper.CornerRadius),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                                RenderTransformOrigin="0.5,0.5">
                                            <Border.RenderTransform>
                                                <ScaleTransform x:Name="scale"
                                                                ScaleX="1"
                                                                ScaleY="1"></ScaleTransform>
                                            </Border.RenderTransform>
                                        </Border>
                                        <Rectangle HorizontalAlignment="Left"
                                                   Fill="White"
                                                   Width="3"
                                                   Margin="5,5,0,5" />
                                        <Border x:Name="RectToggle"
                                                Height="{Binding Path=(local:RadioButtonHelper.BoxHeight),Converter={StaticResource Minus2Converter},RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                                Width="{Binding Path=(local:RadioButtonHelper.BoxHeight),Converter={StaticResource Minus2Converter},RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Center"
                                                Background="{Binding Path=(local:RadioButtonHelper.GlyphBrush),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                                CornerRadius="{Binding Path=(local:RadioButtonHelper.CornerRadius),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}">
                                            <Border.Margin>
                                                <MultiBinding Converter="{StaticResource SwitchToggleMarginConverter}">
                                                    <Binding Path="Width"
                                                             ElementName="Rect" />
                                                    <Binding Path="Height"
                                                             ElementName="Rect" />
                                                </MultiBinding>
                                            </Border.Margin>
                                            <Border.Effect>
                                                <DropShadowEffect x:Name="effect"
                                                                  Color="DimGray"
                                                                  Opacity="0.4"
                                                                  BlurRadius="3"
                                                                  ShadowDepth="1"
                                                                  Direction="275" />
                                            </Border.Effect>
                                        </Border>
                                    </Grid>
                                    <ContentPresenter x:Name="Presenter"
                                                      Grid.Column="1"
                                                      ContentSource="Content"
                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      Margin="{TemplateBinding Padding}" />
                                </Grid>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsChecked, RelativeSource={RelativeSource Self}}"
                                                   Value="True" />
                                        <Condition Binding="{Binding Path=(local:RadioButtonHelper.CheckedContent),Converter={StaticResource IsNotNullConverter}, RelativeSource={RelativeSource Self}}"
                                                   Value="True" />
                                    </MultiDataTrigger.Conditions>
                                    <Setter TargetName="Presenter"
                                            Property="Content"
                                            Value="{Binding Path=(local:RadioButtonHelper.CheckedContent), RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}" />
                                </MultiDataTrigger>
                                <Trigger Property="IsChecked"
                                         Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ThicknessAnimation Storyboard.TargetName="RectToggle"
                                                                    Storyboard.TargetProperty="Margin"
                                                                    To="0,0,0,0"
                                                                    Duration="0:0:0.3">
                                                    <ThicknessAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut" />
                                                    </ThicknessAnimation.EasingFunction>
                                                </ThicknessAnimation>
                                                <DoubleAnimation Storyboard.TargetName="scale"
                                                                 Storyboard.TargetProperty="ScaleX"
                                                                 To="0"
                                                                 Duration="0:0:0.2">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut" />
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                                <DoubleAnimation Storyboard.TargetName="scale"
                                                                 Storyboard.TargetProperty="ScaleY"
                                                                 To="0"
                                                                 Duration="0:0:0.3">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut" />
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ThicknessAnimation Storyboard.TargetName="RectToggle"
                                                                    Storyboard.TargetProperty="Margin"
                                                                    Duration="0:0:0.3">
                                                    <ThicknessAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut" />
                                                    </ThicknessAnimation.EasingFunction>
                                                </ThicknessAnimation>
                                                <DoubleAnimation Storyboard.TargetName="scale"
                                                                 Storyboard.TargetProperty="ScaleX"
                                                                 Duration="0:0:0.3">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut" />
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                                <DoubleAnimation Storyboard.TargetName="scale"
                                                                 Storyboard.TargetProperty="ScaleY"
                                                                 Duration="0:0:0.3">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut" />
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>

                                            </Storyboard>
                                        </BeginStoryboard>

                                    </Trigger.ExitActions>
                                </Trigger>

                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <DataTrigger Binding="{Binding Path=(local:RadioButtonHelper.RadioButtonStyle),RelativeSource={RelativeSource Self}, Mode=OneWay}"
                         Value="Button">
                <Setter Property="VerticalContentAlignment"
                        Value="Center" />
                <Setter Property="HorizontalContentAlignment"
                        Value="Center" />
                <Setter Property="Background"
                        Value="White" />
                <Setter Property="BorderBrush"
                        Value="LightGray" />
                <Setter Property="BorderThickness"
                        Value="0" />
                <Setter Property="Padding"
                        Value="10,0" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type RadioButton}">
                            <Grid x:Name="GrdMain">
                                <Border x:Name="BdrBack"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="{Binding Path=(local:RadioButtonHelper.CornerRadius),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}" />
                                <Border x:Name="BdrHover"
                                        BorderBrush="Transparent"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Background="{Binding Path=(local:RadioButtonHelper.CheckedBackground),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                        CornerRadius="{Binding Path=(local:RadioButtonHelper.CornerRadius),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                        Opacity="0" />
                                <Border x:Name="BdrChecked"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Background="{Binding Path=(local:RadioButtonHelper.CheckedBackground),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                        CornerRadius="{Binding Path=(local:RadioButtonHelper.CornerRadius),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                        Visibility="Collapsed" />
                                <ContentPresenter x:Name="Content"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  Margin="{TemplateBinding Padding}"
                                                  Content="{TemplateBinding Content}"
                                                  TextBlock.Foreground="{TemplateBinding Foreground}" />
                            </Grid>
                            <ControlTemplate.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsChecked, RelativeSource={RelativeSource Self}}"
                                                   Value="True" />
                                        <Condition Binding="{Binding Path=(local:RadioButtonHelper.CheckedContent),Converter={StaticResource IsNotNullConverter}, RelativeSource={RelativeSource Self}}"
                                                   Value="True" />
                                    </MultiDataTrigger.Conditions>
                                    <Setter TargetName="Content"
                                            Property="Content"
                                            Value="{Binding Path=(local:RadioButtonHelper.CheckedContent), RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}" />
                                </MultiDataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsMouseOver,RelativeSource={RelativeSource Self}}"
                                                   Value="True" />
                                        <Condition Binding="{Binding IsChecked,RelativeSource={RelativeSource Self}}"
                                                   Value="False" />
                                    </MultiDataTrigger.Conditions>
                                    <MultiDataTrigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="BdrHover"
                                                                 Storyboard.TargetProperty="Opacity"
                                                                 To="0.2"
                                                                 Duration="0:0:0.3" />
                                            </Storyboard>
                                        </BeginStoryboard>

                                    </MultiDataTrigger.EnterActions>
                                    <MultiDataTrigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="BdrHover"
                                                                 Storyboard.TargetProperty="Opacity"
                                                                 Duration="0:0:0.3" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </MultiDataTrigger.ExitActions>
                                </MultiDataTrigger>
                                <Trigger Property="IsChecked"
                                         Value="True">
                                    <Setter TargetName="Content"
                                            Property="TextBlock.Foreground"
                                            Value="{Binding Path=(local:RadioButtonHelper.CheckedGlyphBrush),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}" />
                                    <Setter TargetName="BdrChecked"
                                            Property="Visibility"
                                            Value="Visible" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <DataTrigger Binding="{Binding Path=(local:RadioButtonHelper.RadioButtonStyle),RelativeSource={RelativeSource Self}, Mode=OneWay}"
                         Value="Selector">
                <Setter Property="local:RadioButtonHelper.CheckedBackground"
                        Value="#3E3E3E" />
                <Setter Property="local:RadioButtonHelper.CheckedGlyphBrush"
                        Value="#FFFFFF" />
                <Setter Property="VerticalContentAlignment"
                        Value="Stretch" />
                <Setter Property="HorizontalContentAlignment"
                        Value="Stretch" />
                <Setter Property="Background"
                        Value="Transparent" />
                <Setter Property="BorderThickness"
                        Value="2" />
                <Setter Property="Padding"
                        Value="0" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type RadioButton}">
                            <Grid>
                                <ContentPresenter x:Name="Content"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  Margin="{TemplateBinding Padding}"
                                                  Content="{TemplateBinding Content}"
                                                  TextBlock.Foreground="{TemplateBinding Foreground}" />
                                <Border x:Name="BdrChecked"
                                        BorderBrush="{Binding Path=(local:RadioButtonHelper.CheckedBackground),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Visibility="Collapsed" />
                                <Grid x:Name="GrdChecked" Height="{Binding Path=(local:RadioButtonHelper.BoxHeight),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                      Width="{Binding Path=(local:RadioButtonHelper.BoxWidth),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                      Background="{Binding Path=(local:RadioButtonHelper.CheckedBackground),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                      VerticalAlignment="Bottom"
                                      HorizontalAlignment="Right"
                                      Visibility="Collapsed">
                                    <Path x:Name="Path"
                                          Width="{Binding Path=(local:RadioButtonHelper.BoxWidth),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                          Height="{Binding Path=(local:RadioButtonHelper.BoxHeight),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                          Stroke="{Binding Path=(local:RadioButtonHelper.CheckedGlyphBrush),RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}"
                                          StrokeThickness="1.5"
                                          StrokeDashCap="Round">
                                        <Path.Data>
                                            <MultiBinding Converter="{StaticResource CheckBoxGlyphPathConverter}">
                                                <Binding Path="ActualWidth"
                                                         ElementName="Path" />
                                                <Binding Path="ActualHeight"
                                                         ElementName="Path" />
                                            </MultiBinding>
                                        </Path.Data>
                                    </Path>
                                </Grid>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsChecked, RelativeSource={RelativeSource Self}}"
                                                   Value="True" />
                                        <Condition Binding="{Binding Path=(local:RadioButtonHelper.CheckedContent),Converter={StaticResource IsNotNullConverter}, RelativeSource={RelativeSource Self}}"
                                                   Value="True" />
                                    </MultiDataTrigger.Conditions>
                                    <Setter TargetName="Content"
                                            Property="Content"
                                            Value="{Binding Path=(local:RadioButtonHelper.CheckedContent), RelativeSource={RelativeSource AncestorType=RadioButton}, Mode=OneWay}" />
                                </MultiDataTrigger>
                                <Trigger Property="IsChecked"
                                         Value="True">
                                    <Setter TargetName="BdrChecked"
                                            Property="Visibility"
                                            Value="Visible" />
                                    <Setter TargetName="GrdChecked"
                                            Property="Visibility"
                                            Value="Visible" />
                                </Trigger>
                            </ControlTemplate.Triggers>

                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </DataTrigger>

            <Trigger  Property="IsEnabled"
                      Value="False">
                <Setter Property="Opacity"
                        Value="0.4" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>